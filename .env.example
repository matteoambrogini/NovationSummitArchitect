# The application runs without credentials through the mock provider.
# Reserved for a future backend-only adapter. Never expose it as VITE_OPENAI_API_KEY.
OPENAI_API_KEY=
VITE_SPOTIFY_CLIENT_ID=
VITE_YOUTUBE_API_KEY=
